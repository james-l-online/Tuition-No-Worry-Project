Archive for old SQL files and backups.

This folder contains SQL patches, large seed variants, and JSON backups that are no longer used by the runtime.

Use this to keep the `sql/` root small and only contain runtime files: `schema.sql` and `seed-full.sql`.

If you need a file restored, move it back to `sql/` and commit.
