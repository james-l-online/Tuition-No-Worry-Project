/*
Demo: Terraform versions file (commented out)

This file is intentionally commented out — it's a scaffold for a demo project.
Do NOT execute these blocks; they are for review and documentation only.
*/

/*
terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = ">= 3.0.0"
    }
    kubernetes = {
      source  = "hashicorp/kubernetes"
      version = ">= 2.0.0"
    }
    random = {
      source  = "hashicorp/random"
      version = ">= 3.0.0"
    }
  }

  required_version = ">= 1.2.0"
}
*/
